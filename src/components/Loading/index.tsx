import { SpinnerContainer, LoadingSpinner } from "./styles";

export default function Loading() {
  return (
    <SpinnerContainer>
      <LoadingSpinner></LoadingSpinner>
    </SpinnerContainer>
  );
}
